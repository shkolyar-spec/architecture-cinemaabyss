name: Docker Build and Push

on:
  workflow_dispatch:
  push:
    branches: [ main, cinema ]
    paths:
      - "src/**"
      - ".github/workflows/docker-build-push.yml"

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  build-test-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build stack for tests
        run: docker compose up -d --build

      - name: Wait for services
        run: |
          echo "Waiting for services to become healthy..."
          for i in {1..60}; do
            curl -fsS http://localhost:8080/health && break || true
            sleep 2
          done

          curl -fsS http://localhost:8080/health
          curl -fsS http://localhost:8081/api/movies/health
          curl -fsS http://localhost:8082/api/events/health
          curl -fsS http://localhost:8000/health

      - name: Run Postman tests (CI)
        working-directory: tests/postman
        run: |
          npm ci
          node run-tests.js --environment ci

      - name: Stop stack
        if: always()
        run: docker compose down -v

      - name: Build and push Events Service image
        uses: docker/build-push-action@v6
        with:
          context: ./src/microservices/events
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/events-service:latest

      - name: Build and push Proxy Service image
        uses: docker/build-push-action@v6
        with:
          context: ./src/microservices/proxy
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/proxy-service:latest
